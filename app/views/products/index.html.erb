<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.name, product_path(product) %> $<%= product.cost %>.00 <%= product.origin %></li>
  <% end %>
</ul>

<%= link_to "Create", new_product_path %>
